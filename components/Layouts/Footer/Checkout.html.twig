<footer class="{{ ('FooterCheckout ' ~ attributes.render('class'))|tailwind_merge }}" {{ attributes.without('class') }}>
    <ul class="FooterCheckout-links">
        {% set contents = resources('/api/front/contents', {'contentFolders.folder.id': attr('config', 'information_folder_id'), visible: true}) %}
        {% for content in contents %}
            <li class="paragraph-5">
                <a href="{{ content.publicUrl }}" class="Footer-link">{{ content.i18ns.title }}</a>
            </li>
        {% endfor %}
    </ul>
    {# Same latin line as the main footer, kept left to right whole for the same reason. #}
    <span class="FooterCheckout-copyright text-grey indication indication-small">
        <span dir="ltr">© <a href="https://www.openstudio.fr/">OpenStudio</a> - {{ 'now'|date('Y') }}</span>
    </span>
</footer>
