<div class="Story">
<div class="Story-section">
    <h4 class="Story-title">Base (LiveComponent — activation code form)</h4>
    <span class="Story-label">
        The account comes from the pending registration in the session, never from a prop, so
        submitting here reports that no registration was found — the real flow is reached from
        <code>/customer/activation</code> after the second registration step.
    </span>
    <div class="Story-stage Story-stage--narrow">
        <twig:Forms:RegisterValidationCode:Base />
    </div>
</div>
</div>
