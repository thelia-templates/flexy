{% use '@formTwig/flexy_form_theme.html.twig' %}

<div class='FieldInput-blockInput'>
		<textarea {{ block('widget_attributes') }} {% if value is not empty %} value="{{ value }}" {% endif %}></textarea>
</div>
